

var RequestResponseApi= function () {

};

RequestResponseApi.prototype.request = function(uri, payload) {

    //generate a promise
    //stand up the client protocol stack.
    //open a client channel on the protocol and attach.
    //fire the client protocol
};

RequestResponseApi.prototype.handleRequest = function() {

    //generate a promise
    //stand up the client protocol stack.
    //open a client channel on the protocol and attach.
    //fire the client protocol
};

module.exports = RequestResponseApi;
