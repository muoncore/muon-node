

require('mocha-sinon');
var sinon = require("sinon");
var assert = require('assert');
var url = require('url');
var _ = require('underscore');

describe("Simulation: Request Response", function () {

    beforeEach(function() {
    });

    //afterEach(function() {
    //    clock.restore();
    //});

    it("emits SUBSCRIBE when requested to", function () {
        assert.ok(false, "AMQP TRANSPORT NOT IMPLEMENTED")
    });
});
